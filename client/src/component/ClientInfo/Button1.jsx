import React from 'react';

const Button1 = ({ children, className, ...props }) => (
  <button1
    className={`buttons1 ${className}`}
    {...props}
  >
    {children}
  </button1>
);

export default Button1;

